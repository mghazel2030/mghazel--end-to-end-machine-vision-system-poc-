# Runtime dependencies — Step 2
# Prefer the editable project install: python -m pip install -e ".[dev]"
PyYAML>=6.0,<7                 # YAML configuration
numpy>=1.26,<3                 # Deterministic numerical simulation
opencv-python-headless>=4.9,<5 # Image synthesis/masks without GUI dependency
matplotlib>=3.8,<4             # Headless QA/report figures

# Development / CI
pytest>=8,<10                  # Unit/integration tests
ruff>=0.9,<1                   # Import/style/static checks

# Step 3 planned: PyTorch/torchvision will be added with CPU/CUDA-specific
# installation guidance only when the hybrid CV/PyTorch feature is implemented.
